dist: trusty
sudo: required

addons:
    apt:
      packages:
        - xvfb

services:
  - docker

install:
  - export DISPLAY=':99.0'
  - Xvfb :99 -screen 0 1024x768x24 > /dev/null 2>&1 &
  - sudo apt -y -qq install python

script:
 - ./do.sh build
