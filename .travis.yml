dist: trusty
sudo: required

install:
 - sudo $TRAVIS_BUILD_DIR/tools/build/install.sh

script:
 - bazel test //src/...
 - python $TRAVIS_BUILD_DIR/src/vision/test_vision.py
 - python $TRAVIS_BUILD_DIR/src/control/test_control.py
