# required metadata
sonar.projectKey=drone_code
sonar.projectName=drone_code
sonar.projectVersion=1.0

# optional description
sonar.projectDescription=Code for competing in the SUAS competition.

# path to source directories (required)
sonar.sources=src,lib,tools/scripts
sonar.host.url=https://uasatucla.org/sonarqube

# path to test source directories (optional)
#sonar.tests=

# path to project binaries (optional), for example directory of Java bytecode
#sonar.binaries=binDir

# language configurations
sonar.lang.patterns.c=-
sonar.cxx.cppcheck.reportPath=/tmp/cppcheck-report.xml
